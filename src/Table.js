import React from 'react';
import { useTable, useFilters, useSortBy, usePaginition} from 'react-table';

function Table({ selectedDate }) {

// Пример настройки: https://react-table.tanstack.com/docs/quick-start

  return (

    // Верните JSX таблицы с фильтрами и пагинацией

  );
}

export default Table;